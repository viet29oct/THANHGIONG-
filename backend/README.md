# Licensing API (Spring Boot)

## Run

```bash
docker compose up -d
cd backend
mvn spring-boot:run
```

Env: `DB_*`, `JWT_SECRET`, `LICENSE_AES_KEY` (32 chars), `FRONTEND_URL`, `SMTP_*`.

## Default port

8080
